<tr>
  <td width="5%"></td>
  <td><%= name %></td>

  <!-- [Ã±·î] -->
  <!-- ºòÇ¯¼ÂÀÓ -->
  <td align="right">
    <%= number_to_currency(zennen, :precision => 0, :unit => '') %>
  </td>
  <!-- Í½»» -->
  <td align="right">
    <%= number_to_currency(yosan, :precision => 0, :unit => '') %>
  </td>
  <!-- ¼ÂÀÓ -->
  <td align="right">
    <b><%= number_to_currency(zisseki, :precision => 0, :unit => '') %></b>
  </td>
  <!-- ¼ÂÇäÎ¨ -->
  <% brate = sprintf("%.1f", zisseki * 100.0 / uriage) %>
  <td align="right"><%= brate %></td>
  <!-- ºòÇ¯ÂÐÈæ -->
  <td align="right">
    <%= number_to_currency(zisseki - zennen, :precision => 0, :unit => '') %>
  </td>
  <!-- ºòÂÐÎ¨ -->
  <% srate = sprintf("%.1f", zisseki * 100.0 / zennen) %>
  <td align="right"><%= srate %></td>
  <!-- Í½»»ÂÐÈæ -->
  <td align="right">
    <%= number_to_currency(zisseki - yosan, :precision => 0, :unit => '') %>
  </td>
  <!-- ÌÜÉ¸Î¨ -->
  <% yrate = sprintf("%.1f", zisseki * 100.0 / yosan) %>
  <td align="right"><%= yrate %></td>
  <!-- ·ÐÍý -->
  <td align="right">
    <%= number_to_currency(keiri, :precision => 0, :unit => '') %>
  </td>
  <!-- ·ÐÍýº¹³Û -->
  <td align="right">
    <%= number_to_currency(zisseki - keiri, :precision => 0, :unit => '') %>
  </td>
  <!-- ·ÐÍýÂÐÈæ -->
  <% krate = sprintf("%.1f", zisseki * 100.0 / keiri) %>
  <td align="right"><%= krate %></td>

  <!-- [Îß·×] -->
  <!-- ºòÇ¯¼ÂÀÓ -->
  <td align="right">
    <%= number_to_currency(czennen, :precision => 0, :unit => '') %>
  </td>
  <!-- Í½»» -->
  <td align="right">
    <%= number_to_currency(cyosan, :precision => 0, :unit => '') %>
  </td>
  <!-- ¼ÂÀÓ -->
  <td align="right">
    <b><%= number_to_currency(czisseki, :precision => 0, :unit => '') %></b>
  </td>
  <!-- ¼ÂÇäÎ¨ -->
  <% brate = sprintf("%.1f", czisseki * 100.0 / curiage) %>
  <td align="right"><%= brate %></td>
  <!-- ºòÇ¯ÂÐÈæ -->
  <td align="right">
    <%= number_to_currency(czisseki - czennen, :precision => 0, :unit => '') %>
  </td>
  <!-- ºòÂÐÎ¨ -->
  <% srate = sprintf("%.1f", czisseki * 100.0 / czennen) %>
  <td align="right"><%= srate %></td>
  <!-- Í½»»ÂÐÈæ -->
  <td align="right">
    <%= number_to_currency(czisseki - cyosan, :precision => 0, :unit => '') %>
  </td>
  <!-- ÌÜÉ¸Î¨ -->
  <% yrate = sprintf("%.1f", czisseki * 100.0 / cyosan) %>
  <td align="right"><%= yrate %></td>
  <!-- ·ÐÍý -->
  <td align="right">
    <%= number_to_currency(ckeiri, :precision => 0, :unit => '') %>
  </td>
  <!-- ·ÐÍýº¹³Û -->
  <td align="right">
    <%= number_to_currency(czisseki - ckeiri, :precision => 0, :unit => '') %>
  </td>
  <!-- ·ÐÍýÂÐÈæ -->
  <% krate = sprintf("%.1f", czisseki * 100.0 / ckeiri) %>
  <td align="right"><%= krate %></td>
</tr>
